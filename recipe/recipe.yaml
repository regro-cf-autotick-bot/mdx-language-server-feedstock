context:
  version: "0.6.2"

package:
  name: mdx-language-server
  version: ${{ version }}

source:
  url: https://registry.npmjs.org/@mdx-js/language-server/-/language-server-${{ version }}.tgz
  sha256: c2edf77df7c42662e3ec638e8ee7ff9ef0a41f664374267dcbd88d9c5610e6cb

build:
  number: 0
  noarch: generic

requirements:
  build:
    - nodejs
    - pnpm
    - pnpm-licenses
  run:
    - nodejs

tests:
  - script:
      - "mdx-language-server 2>&1 | grep -q 'Connection input stream is not set.'"

about:
  license: MIT
  license_file:
    - LICENSE
    - third-party-licenses.txt
  summary: Language server for MDX
  homepage: https://github.com/mdx-js/${{ name }}
  repository: https://github.com/mdx-js/${{ name }}

extra:
  recipe-maintainers:
    - danielnachun
